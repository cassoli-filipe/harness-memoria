{
  "$comment": "Config MÍNIMA de exemplo — só os campos que costumam DIFERIR do default no primeiro projeto (o exemplo real é o objeto abaixo: `projeto`, `reafirmacao.sub_bloco`, `formatadores`). Todo campo tem default: escreva só o que difere, porque config que repete o default mente sobre ter sido pensada — os seis campos que este arquivo tinha antes e removeu (`diario.skill_de_encerramento`, `diario.injetar_ultima_entrada`, `adr.secao_plano`, `adr.primeiro_com_regra`, `guardas.caminhos`, `guardas.comandos`) eram, um a um, idênticos ao default. A presença deste arquivo é o gate — sem ele, todo hook do harness é inerte neste projeto. Referência completa dos campos, com o incidente que justificou cada um: `src/harness_memoria/config.py` no repo do harness.",
  "projeto": "Nome do projeto",

  "reafirmacao": { "sub_bloco": "**NUNCA**" },

  "formatadores": [
    { "extensoes": [".py"], "comando": ["ruff", "format", "{arquivo}"] }
  ]
}
